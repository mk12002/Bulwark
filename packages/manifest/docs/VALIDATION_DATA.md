# Manifest validation data

Generated by `python packages/manifest/scripts/study.py`. Every number below is
reproduced from that command; nothing here is hand-written.

## 1. Discovery recall vs. hand-written ground truth

Ground truth is written from the fixture trees by hand, not from Manifest's own
output — a self-comparison would only prove the tool agrees with itself. Recall is
measured on component *identity*; the type assignment is reported separately below,
because whether `torch` is a `library` or a `framework` is a modelling choice rather
than a discovery failure.

| Project | Expected | Found | Recall | Missed | Also reported |
| --- | :---: | :---: | :---: | --- | --- |
| `sample_project_clean` | 4 | 4 | 100% | — | — |
| `sample_project_risky` | 11 | 11 | 100% | — | — |

**Overall recall: 15/15 = 100%.**

Where each expected component comes from, and the type Manifest assigned it:

| Project | Component | Present in the fixture as | Discovered | Typed as |
| --- | --- | --- | :---: | --- |
| `sample_project_clean` | `model.safetensors` | model/ weights | pass | machine-learning-model |
| `sample_project_clean` | `numpy` | requirements.txt | pass | library |
| `sample_project_clean` | `torch` | requirements.txt | pass | framework |
| `sample_project_clean` | `transformers` | requirements.txt | pass | framework |
| `sample_project_risky` | `datasets` | explore.ipynb — !pip install | pass | library |
| `sample_project_risky` | `google/flan-t5-small` | explore.ipynb — from_pretrained() | pass | machine-learning-model |
| `sample_project_risky` | `imdb` | explore.ipynb — load_dataset() | pass | data |
| `sample_project_risky` | `model.safetensors` | model/ weights | pass | machine-learning-model |
| `sample_project_risky` | `openai` | requirements.txt | pass | framework |
| `sample_project_risky` | `pytorch_model.bin` | model/ weights (pickle) | pass | machine-learning-model |
| `sample_project_risky` | `pyyaml` | requirements.txt | pass | library |
| `sample_project_risky` | `system_prompt` | agent.yaml — embedded prompt | pass | data |
| `sample_project_risky` | `torch` | explore.ipynb — !pip install | pass | library |
| `sample_project_risky` | `train.csv` | data/ on disk | pass | data |
| `sample_project_risky` | `transformers` | requirements.txt (unpinned) | pass | framework |

*Also reported* lists components Manifest found that ground truth did not name.
These are not false positives by default — but each is listed so the gap is auditable.

## 2. BOM conformance

Required-field and well-formedness conformance for both output formats. This is a
structural check against the specs' required fields, not a full JSON-Schema
validation against the upstream schema documents (which would require network I/O).

| Check | Detail | Result |
| --- | --- | :---: |
| CycloneDX document required fields | bomFormat, specVersion, version | pass |
| bomFormat is 'CycloneDX' | CycloneDX | pass |
| serialNumber is a urn:uuid | urn:uuid:17dad24d-75c8-7f7f-fad3-baa2fc27fded | pass |
| every component has type+name | 11 components | pass |
| component types are in the CycloneDX enum | used: ['data', 'framework', 'library', 'machine-learning-model'] | pass |
| dependency refs resolve to a component | 0 dependency entries | pass |
| SPDX document required fields | spdxVersion, SPDXID, name, documentNamespace, creationInfo | pass |
| SPDXID is SPDXRef-DOCUMENT | SPDXRef-DOCUMENT | pass |
| every package has SPDXID+name | 11 packages | pass |
| package SPDXIDs use the SPDXRef- prefix | 11 packages | pass |

**10/10 conformance checks pass.**

## 3. Governance coverage

How much of each control framework a single real scan actually exercises.

| Framework | Controls | Exercised by this project | Controls |
| --- | :---: | :---: | --- |
| NIST AI RMF | 4 | 3 | GOVERN (8), MANAGE (0), MAP (7), MEASURE (2) |
| EU AI Act | 6 | 5 | Art.10 Data governance (6), Art.11 Technical documentation (6), Art.12 Record-keeping (4), Art.13 Transparency (5), Art.14 Human oversight (0), Art.15 Accuracy, robustness & cybersecurity (2) |

Per-code breakdown for `sample_project_risky`:

| B-code | Findings | Mapped to a control |
| --- | :---: | :---: |
| B1 | 5 | pass |
| B3 | 5 | pass |
| B4 | 1 | pass |
| B6 | 1 | pass |
| B7 | 1 | pass |
| B8 | 1 | pass |
| B9 | 3 | pass |

## 4. Risk-bridge fidelity

`--scan-risk` runs Airlock and Warden on discovered components and folds their
findings in as B5. The failure modes measured here are silent drop and double-counting.

| Metric | Value | Result |
| --- | --- | :---: |
| baseline findings (no --scan-risk) | 14 (6 codes) | pass |
| bridged findings (--scan-risk) | 21 (10 codes) | pass |
| B5 risk findings folded in | 1 | pass |
| baseline codes preserved under bridging | lost: none | pass |
| no duplicate (rule, location) pairs | 21 findings, 21 unique | pass |
| bridged findings carry a severity | max=high | pass |

**6/6 bridge checks pass.**


