[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "bulwark-airlock"
version = "0.2.0"
description = "Static security scanner for the AI agent supply chain — audit ML models and MCP servers before they touch your agent."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Apache-2.0" }
authors = [{ name = "Airlock contributors" }]
keywords = ["security", "mcp", "machine-learning", "supply-chain", "scanner", "sarif"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Security",
]
dependencies = [
    "bulwark-core",
    "typer>=0.12",
    "rich>=13.7",
    "pydantic>=2.6",
    "pydantic-settings>=2.2",
    "pyyaml>=6.0",
    "jinja2>=3.1",
]

[project.optional-dependencies]
model = [
    "huggingface_hub>=1.27.0",
]
mcp = [
    "mcp>=1.0",
]
ai = [
    "httpx>=0.27",
]
dev = [
    "ruff>=0.5",
    "mypy>=1.10",
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "types-PyYAML>=6.0",
    "huggingface_hub>=1.27.0",
    "mcp>=1.0",
    "httpx>=0.27",
]

[project.scripts]
airlock = "airlock.cli:app"

[project.urls]
Homepage = "https://github.com/mk12002/Bulwark"
Repository = "https://github.com/mk12002/Bulwark"
Documentation = "https://github.com/mk12002/Bulwark/blob/main/docs/PROJECT_REFERENCE_AIRLOCK.md"
Issues = "https://github.com/mk12002/Bulwark/issues"
Changelog = "https://github.com/mk12002/Bulwark/blob/main/CHANGELOG.md"

[tool.hatch.build.targets.wheel]
packages = ["src/airlock"]

[tool.ruff]
line-length = 100
target-version = "py311"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "C4", "RUF"]
ignore = [
    "B008",    # typer uses function calls in argument defaults
    "RUF001",  # prose/docstrings deliberately use en/em dashes and unicode
    "RUF002",
    "RUF003",
]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["B011"]

[tool.mypy]
python_version = "3.11"
warn_unused_configs = true
disallow_untyped_defs = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
strict_equality = true

[[tool.mypy.overrides]]
module = ["airlock.scanners.*"]
disallow_untyped_defs = true
disallow_incomplete_defs = true
check_untyped_defs = true
warn_return_any = true

[[tool.mypy.overrides]]
module = [
    "huggingface_hub.*",
    "mcp.*",
    "yaml.*",
]
ignore_missing_imports = true

[tool.pytest.ini_options]
minversion = "8.0"
testpaths = ["tests"]
addopts = "-q"
markers = [
    "integration: tests that spawn a live MCP fixture server over stdio",
]

[tool.coverage.run]
source = ["src/airlock"]
branch = true
