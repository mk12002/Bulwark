"""Bulwark — the security stack for agentic AI.

A thin meta-CLI that mounts the three tools under one command and adds a
whole-system ``bulwark scan`` that runs the full inventory + risk pipeline.
"""

from __future__ import annotations

__version__ = "0.2.0"
