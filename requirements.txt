# Editable dev install of the Bulwark workspace.
#   pip install -r requirements.txt
#
# Installs the shared core first, then each tool with its extras and dev toolchain.
-e ./packages/bulwark-core
-e ./packages/airlock[dev,model,mcp,ai]
-e ./packages/warden[dev]
